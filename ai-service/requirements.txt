fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
numpy==1.26.4
python-multipart==0.0.9
httpx==0.27.2
openai==1.51.0
faiss-cpu==1.8.0
sentence-transformers==3.0.1
pytest==8.3.3
pytest-asyncio==0.24.0

# --- MODULE 3: RAG / Vector Search (all optional, gracefully degrade to local fallback) ---
google-cloud-aiplatform==1.68.0
chromadb==0.5.15
tenacity==9.0.0
tiktoken==0.8.0


python-dotenv==1.0.1

# --- MODULE 8: Evaluation Framework (OPTIONAL) -------------------------------
# ImpactAnalyserEvaluator (app/evaluation/evaluator.py) is fully deterministic
# and requires NONE of the packages below to run. They are only needed if you
# inject RagasFaithfulnessStrategy as a supplementary LLM-as-judge opinion:
#   pip install ragas datasets
# ragas==0.2.8
# datasets==3.0.1
